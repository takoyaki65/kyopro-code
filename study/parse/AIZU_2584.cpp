#include <bits/stdc++.h>
using namespace std;

string Cipher(string& s, int& i);
string String(string& s, int& i);
char Letter(string& s, int& i);

string Cipher(string& s, int& i);

int main() {
    return 0;
}